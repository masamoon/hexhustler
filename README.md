# HexHustler

## Web deploy

Pushing to `main` runs `.github/workflows/deploy-pages.yml`, exports the Godot Web preset, and deploys `build/web` to GitHub Pages.

In the GitHub repository settings, set **Pages > Build and deployment > Source** to **GitHub Actions**.
